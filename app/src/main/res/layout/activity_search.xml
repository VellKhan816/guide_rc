<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/darkfifty"> <!-- Используем тот же фон -->

    <!-- Верхняя панель с кнопкой "Назад" и полем поиска -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_gradient_dark_blue_overlay"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Кнопка "Назад" -->
        <ImageView
            android:id="@+id/backButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_return"
            app:tint="#FFFFFF"
            android:contentDescription="Назад" />

        <!-- Поле ввода поискового запроса -->
        <EditText
            android:id="@+id/searchInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:background="@drawable/button_purple_rounded"
            android:hint="Поиск историй..."
            android:padding="12dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#B0B0B0"
            android:inputType="text"
            android:imeOptions="actionSearch" /> <!-- Опционально: добавляет кнопку поиска на клавиатуре -->

    </LinearLayout>

    <!-- RecyclerView для результатов поиска -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewSearchResults"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="12dp"
        android:clipToPadding="false"
        android:scrollbars="vertical" />

</LinearLayout>